<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="TabDarkStyle" TargetType="TabControl">
        <Setter Property="Background" Value="#121112" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style x:Key="TabItemDarkStyle" TargetType="TabItem">
        <Style.Setters>
            <Setter Property="FontFamily" Value="Verdana" />
            <Setter Property="FontSize" Value="13" />

            <Setter Property="Foreground" Value="#b7b7b7" />

            <Setter Property="MinWidth" Value="210" />
            <Setter Property="MinHeight" Value="65" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid Name="Panel" Background="Transparent">
                            <ContentPresenter
                                x:Name="ContentSite"
                                Margin="20,15,15,15"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center"
                                ContentSource="Header" />
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Panel" Property="Background" Value="#121212" />
                                <Setter Property="Foreground" Value="#e0e0e0" />
                                <Setter Property="FontSize" Value="15" />
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="FontSize" Value="15" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>
</ResourceDictionary>